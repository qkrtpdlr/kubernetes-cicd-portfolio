apiVersion: v1
kind: Secret
metadata:
  name: fresh-chicken-secret
  namespace: production
type: Opaque
stringData:
  # Database Credentials
  DB_USERNAME: "admin"
  DB_PASSWORD: "change-this-password"  # 실제 환경에서는 AWS Secrets Manager 사용
  
  # Redis Password (ElastiCache Auth Token)
  REDIS_PASSWORD: ""
